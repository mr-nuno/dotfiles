## Terminal Output Style

When asking a question or requesting clarification in the terminal, always prefix it with a ❓ emoji.

Examples:
- ❓ Should I create a separate command handler or reuse the existing one?
- ❓ Do you want the endpoint to return a 204 or the updated resource?
- ❓ Which database should this migration target?